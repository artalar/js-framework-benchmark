diff --git a/packages/jsx/src/index.test.tsx b/packages/jsx/src/index.test.tsx
index f3d2158..e58f8d8 100644
--- a/packages/jsx/src/index.test.tsx
+++ b/packages/jsx/src/index.test.tsx
@@ -158,6 +158,7 @@ test('dynamic children', () =>
 
 test('on: handlers run under shared jsxEvent action', () =>
   context.start(async () => {
+    DEBUG.set(true)
     let clickActionName = ''
     let moveActionName = ''
     let labels: string[] = []
diff --git a/packages/jsx/src/index.ts b/packages/jsx/src/index.ts
index 6d6eab2..8bd2add 100644
--- a/packages/jsx/src/index.ts
+++ b/packages/jsx/src/index.ts
@@ -89,7 +89,7 @@ let eventActionName = (element: Node, eventKey: string, handler: Fn) => {
 
 export let jsxEvent = action(
   (name: string, event: Event, node: Node, handler: Fn) => {
-    if (!/\._/.test(name)) log.label(name, event, node)
+    if (peek(DEBUG) && !/\._/.test(name)) log.label(name, event, node)
     try {
       let result = handler(event)
       ;(result as PromiseLike<unknown>)?.then?.(undefined, (error: unknown) => {
@@ -787,7 +787,9 @@ let setProp = (dom: DomApis, element: JSX.Element, key: string, value: any) => {
   /** @todo Show warning if isAtom(value) && !isAction(value). */
   if (key.startsWith('on:')) {
     key = key.slice(3)
-    let name = eventActionName(element, key, value)
+    // When DEBUG is off, skip `eventActionName` work; `._` also keeps
+    // `log.label` gated inside `jsxEvent` from building label payloads.
+    let name = peek(DEBUG) ? eventActionName(element, key, value) : `._${key}`
     // Bind to the root frame — not `top()`. Row render often runs inside
     // `reatomMap`'s computed; capturing that frame would pin its pre-`_copy`
     // state (createMany `nodes` / head→tail) for as long as the listener
