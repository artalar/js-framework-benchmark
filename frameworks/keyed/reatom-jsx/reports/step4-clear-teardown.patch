diff --git a/packages/jsx/src/index.ts b/packages/jsx/src/index.ts
index a385aa0..6d6eab2 100644
--- a/packages/jsx/src/index.ts
+++ b/packages/jsx/src/index.ts
@@ -447,8 +447,16 @@ let walkLinkedList = (
       connectAppendedRoots(roots, symbol)
   }
 
+  // Clear / version-jump: unsubscribe row trees here instead of waiting for MO
+  // to walk the removed subtree after `innerHTML = ''`.
+  let teardownLinkedListChildren = () => {
+    if (element.childNodes.length === 0) return
+    cleanupNodes(Array.from(element.childNodes), symbol)
+  }
+
   let cb = (state: LinkedList<LLNode<JSX.Element>>) => {
     if (state.version - 1 > lastVersion) {
+      teardownLinkedListChildren()
       element.innerHTML = ''
       let rebuildBatch = dom.document.createDocumentFragment()
       let rebuildRoots: Node[] = []
@@ -527,6 +535,7 @@ let walkLinkedList = (
             element.prepend(change.node)
           }
         } else if (change.kind === 'clear') {
+          teardownLinkedListChildren()
           element.innerHTML = ''
         }
       }
